suite_namespace: frontend\tests\acceptance
actor: AcceptanceTester
modules:
    enabled:
        - WebDriver:
              url: 'http://localhost/healthify/backend/web'
              window_size: false # disabled in ChromeDriver
              port: 9515
              browser: chrome
              capabilities:
                  chromeOptions:

                      prefs:
                          download.default_directory: "..."
